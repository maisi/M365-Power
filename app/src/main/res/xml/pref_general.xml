<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="logging_switch"
        android:title="Enable logging"
        android:switchTextOff="OFF"
        android:switchTextOn="ON"
        android:summary="Changes active on next app start"/>

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <!--<EditTextPreference-->
        <!--android:capitalize="words"-->
        <!--android:defaultValue="@string/pref_default_display_name"-->
        <!--android:inputType="textCapWords"-->
        <!--android:key="example_text"-->
        <!--android:maxLines="1"-->
        <!--android:selectAllOnFocus="true"-->
        <!--android:singleLine="true"-->
        <!--android:title="@string/pref_title_display_name" />-->

    <ListPreference
        android:defaultValue="600"
        android:entries="@array/default_efficiency_titles"
        android:entryValues="@array/default_efficiency_values"
        android:key="default_efficiency"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/default_efficiency_title"
        android:summary="@string/default_efficiency_summary" />

    <ListPreference
        android:defaultValue="4"
        android:entries="@array/min_speed_values"
        android:entryValues="@array/min_speed_values"
        android:key="min_speed"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/min_speed_title"
        android:summary="@string/min_speed_summary"/>

</PreferenceScreen>
